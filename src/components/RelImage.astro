---
import { ImgAttributes } from 'astro:assets'

interface Props extends ImgAttributes {
    src: string
}

const { src, ...props } = Astro.props
---

<img src={import.meta.env.BASE_URL + src} {...props} />
