---
layout: '@layouts/Layout.astro'
title: 'Galerie'
---

import Gallery from '@components/Gallery.astro'

## Trainingslager

### Gnewikow 2014 (inkl. Prüfung)

<Gallery
    images={[...Array(9).keys()].map((i) => ({
        src: `/photos/camp/gnewikow/2014/${
            i + 1 < 10 ? `0${i + 1}` : i + 1
        }.jpg`,
        alt: 'Trainingslager Gnewikow 2014',
        loading: 'lazy',
    }))}
/>

### Eilenburg 2009

<Gallery
    images={[...Array(6).keys()].map((i) => ({
        src: `/photos/camp/eilenburg/2009/${
            i + 1 < 10 ? `0${i + 1}` : i + 1
        }.jpg`,
        alt: 'Trainingslager Eilenburg 2009',
        loading: 'lazy',
    }))}
/>

### Gnewikow 2007

<Gallery
    images={[...Array(9).keys()].map((i) => ({
        src: `/photos/camp/gnewikow/2007/${
            i + 1 < 10 ? `0${i + 1}` : i + 1
        }.jpg`,
        alt: 'Trainingslager Gnewikow 2007',
        loading: 'lazy',
    }))}
/>

## Vorführungen

### Tag der Kinder und Jugend, Wusterhausen - Juni 2005

<Gallery
    images={[...Array(6).keys()].map((i) => ({
        src: `/photos/demo/2005/${i + 1 < 10 ? `0${i + 1}` : i + 1}.jpg`,
        alt: 'Vorführung Wusterhausen 2005',
        loading: 'lazy',
    }))}
/>

### Holzhausen - August 2011

<div class="container">
    <div class="sm:hidden mx-auto w-60 max-w-full p-4 border-2 shadow-md rounded-lg aspect-video">
        {/* https://feathericons.dev/?search=youtube&iconset=feather */}
        <svg
            class="w-24 max-w-full aspect-square mx-auto"
            xmlns="http://www.w3.org/2000/svg"
            viewBox="0 0 24 24"
            fill="rgb(252, 13, 27)"
            stroke="rgb(252, 13, 27)"
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="0"
        >
            <path d="M22.54 6.42a2.78 2.78 0 0 0-1.94-2C18.88 4 12 4 12 4s-6.88 0-8.6.46a2.78 2.78 0 0 0-1.94 2A29 29 0 0 0 1 11.75a29 29 0 0 0 .46 5.33A2.78 2.78 0 0 0 3.4 19c1.72.46 8.6.46 8.6.46s6.88 0 8.6-.46a2.78 2.78 0 0 0 1.94-2 29 29 0 0 0 .46-5.25 29 29 0 0 0-.46-5.33z" />
            <polygon
                fill="white"
                points="9.75 15.02 15.5 11.75 9.75 8.48 9.75 15.02"
            />
        </svg>
    </div>
    <iframe
        class="hidden sm:block mx-auto w-8/12 aspect-[4/3]"
        src="https://www.youtube-nocookie.com/embed/YC4a09sR7lU"
        title="YouTube video player"
        frameborder="0"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
        allowfullscreen
    />

</div>

## Lehrgänge und Prüfungen

### Prüfung, Wusterhausen - Juli 2009

<Gallery
    images={[...Array(11).keys()].map((i) => ({
        src: `/photos/exam/2009/${i + 1 < 10 ? `0${i + 1}` : i + 1}.jpg`,
        alt: 'Prüfung Wusterhausen 2009',
        loading: 'lazy',
    }))}
/>

### Jiu-Jitsu Lehrgang, Neuruppin - 2006

<Gallery
    images={[...Array(19).keys()].map((i) => ({
        src: `/photos/course/neuruppin/2006/${
            i + 1 < 10 ? `0${i + 1}` : i + 1
        }.jpg`,
        alt: 'Jiu-Jitsu Lehrgang Neuruppin 2006',
        loading: 'lazy',
    }))}
/>
